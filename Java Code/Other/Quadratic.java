import java.lang.*;
import java.util.Scanner;
import java.text.DecimalFormat;

public class Quadratic {
  public static void main(String[] args) {
    /*double a, b, c, discriminant, outPos, outNeg;
    Scanner scan = new Scanner(System.in);
    DecimalFormat fmt = new DecimalFormat("##.###");

    System.out.println("A: ");
    a = scan.nextDouble();
    System.out.println("B: ");
    b = scan.nextDouble();
    System.out.println("C: ");
    c = scan.nextDouble();

    discriminant = Math.pow(b, 2) - (4 * a * c);

    if (discriminant < 0) {
      System.out.println("The roots determined from this equation will be an imaginary number!");
    }
    else  {
    outPos = ((0 - b) + Math.sqrt(discriminant)) / (2 * a);
    outNeg = ((0 - b) - Math.sqrt(discriminant)) / (2 * a);

    System.out.println("The roots are " + fmt.format(outPos) + " and " + fmt.format(outNeg));
}*/
String s1 = "James Gosling";
String s2 = "James Gosling";
if (s1.equals(s2))
     System.out.print("help");
} // main
} // class
